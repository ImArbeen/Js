// Learning the javascript 


// Variables 


{
  let a = 45;
  let b = 45;
  let sum = a+b;
  console.log(sum)
  }
  {
    const a = 34;
    // const cant be reassigned again 
  
  }
  {
    // operators
  
    let x =45;
    let y = 56;
    console.log(`The Sum of the ${x} and ${y} is ${x+y}`)
  }
  
  // Operators 
  
  {
    // Addition (+)
  
    let x =45;
    let y = 56;
    console.log(`The Sum of the ${x} and ${y} is ${x+y}`)
  
    // subtraction (-)
  
    let a =45;
    let b = 56;
    console.log(`The Subtraction of the ${a} and ${b} is ${a-b}`)
  
    // Multiplication (*)
  
    let firstNumber  =45;
    let lastNumber = 56;
    console.log(`The Multiplication of the ${firstNumber} and ${lastNumber} is ${firstNumber*lastNumber}`)
  
    // Exponential (**)
  
    let firsExponentialtNumber  = 45;
    let lastExponentialNumber = 56;
    console.log(`The exponential product of the ${firsExponentialtNumber} and ${lastExponentialNumber} is ${firsExponentialtNumber**lastExponentialNumber}`)
  
  //   Here what actually happens is : firstExponentialnumber get the power of secondExponanetialNumber 

   
  }

  {
      // Division 

      let firstNaturalNumber = 90;
      let lastNaturalNumber = 30;
      Division = firstNaturalNumber/lastNaturalNumber;
      console.log(Division);

      // Modulus 

      // modolus (%) show the how much remain 

      let x = 40;
      let y = 2;
      modolus = x%y;
      console.log(modolus)

      // increament  (++) which basically increse the value of the variable 

      let firstIncreamentNumber =45;
      firstIncreamentNumber++;
      let afterIncreamentedNumber = firstIncreamentNumber;
      console.log(afterIncreamentedNumber);

      // Decreament basically decrease (--)

      {
            let firstNumber =45;
            firstNumber--;
            let afterDecreasementNumber = firstNumber;
            console.log(afterDecreasementNumber);

            
      }  


  }

  // Assignment Operators
  {

      // equal to operator (=) 

      let x = "Arbeen Shrestha ";
      console.log(x)

      // += Operator  (+=)

      let a = 4;
      a+=6;
      console.log(a)

      // -= Operator  (-=)

      let firstNumbner= 6;
      firstNumbner-=3;
      console.log(firstNumbner)

      // *= Operator  (*=)

      let firstNaturalNumber = 4;
      firstNaturalNumber*=6;
      console.log(firstNaturalNumber)

      // /= Operator  (/=)

      let myNumber = 30;
      myNumber/=2;
      console.log(myNumber)

      // %= Operator  (%=)

      let modolusNumber =30;
      modolusNumber %= 2;
      console.log(modolusNumber)

      // **= Operator  (*=)

      let number = 2;
      number*=56;
      console.log(`The result of the operator is ${number}`)

      alert("learn Actively Dont be lazy ")

          
  }

  
