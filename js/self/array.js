const prompt = require(`prompt-sync`)();

// array (arry is the set of the name of the object)

const nameList = ["Arbeen", "Radhika", "Khom",]
nameList.map((item,index,self)=>{
      console.log(item)
})
console.log(nameList)      
